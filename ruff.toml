target-version = "py311"

[lint]
# Start with syntax, invalid control flow, and undefined-name errors.
# Formatting and broader style rules can be introduced separately.
select = ["E9", "F63", "F7", "F82"]
