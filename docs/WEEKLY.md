# ClipForge 每周工作记录

## 第二周：媒体流水线骨架与最小闭环

### 本周目标

完成“上传视频 → 后台处理 → 进度显示 → 下载切片”的最小流程。视频归一化为 CFR 30 fps / H.264 / AAC，按每段最多 30 秒切片。

当前已完成媒体模块、FastAPI 与 Celery/Redis 视频任务接入，以及浏览器上传、历史任务、阶段进度和下载页面。2026-09-22 在实际 Docker 部署中走通页面最小闭环；已补充重启操作后的已完成任务与下载文件对比，结果一致；60 分钟合成素材处理与首次真实 CI 已通过；基本异常输入验收也已完成；完整性能数据和资源故障等场景仍待补齐。

### 本周完成

#### 1. 开发环境与 Git 工作流程

- 使用 Git 命令克隆仓库，核对分支、远程地址和提交历史。
- 创建第二周开发分支 `week2/mvp0`，练习本地修改、检查差异、暂存、提交和远程同步。
- 安装并验证 Python 3.11.9，创建项目独立环境 `.venv`。
- 配置 `.gitignore`，忽略虚拟环境和 Python 缓存文件。

#### 2. 视频信息读取与统一格式

- 实现 `probe.py`：调用 FFprobe，将 JSON 输出解析并整理为项目统一的视频信息结构。
- 新增 `schemas/media_meta.schema.json`，定义字段类型和未知值的表示方式。
- 使用 `Sucai1.mp4` 验证读取结果：时长约 41.27 秒，分辨率 960×600，视频编码 HEVC，平均帧率 30 fps，音频编码 AAC、44.1 kHz、单声道。
- 保存元数据样本 `docs/samples/media_meta.json`。

#### 3. 视频归一化转码

- 实现 `normalize.py`，复用视频信息读取功能，将素材转为 H.264 / AAC、恒定 30 fps 的 MP4。
- 实现原素材保护、已有输出拒绝覆盖、转码超时及失败处理。
- 真实素材转码后，基本参数检查和人工播放检查通过。
- 助手另行检查 Python 转码产物的 1,238 帧时间戳，相邻帧间隔均为 1/30 秒。
- 补充 `docs/NORMALIZATION.md`，记录运行方法、验证结果和当前限制。

#### 4. 固定时长自动切片

- 实现 `split.py`，按每段 900 帧规划切片，在 30 fps 下对应每段最多 30 秒。
- 根据切片清单执行转码，保存 `clip_plan.json`，记录片段边界和帧数。
- 真实归一化素材自动生成两段，经程序检查分别为 900 帧和 338 帧。
- 补充 `docs/SPLITTING.md`，记录运行方法与验证范围。

#### 5. 自动化验证

- 媒体模块 22 项单元测试通过：7 项元数据转换测试、7 项转码约束与文件保护测试、8 项切片边界与目录保护测试。
- 新增 7 项加法接口测试、10 项视频任务测试，覆盖提交响应、队列异常、状态记录、上传校验、下载保护与重复任务处理。与媒体测试合计 39 项，2026-09-21 在本地仓库运行全部通过。接口单元测试使用队列或媒体处理替身，真实素材验证另行记录。
- 运行 `tests/integration_split.py`，真实调用 FFmpeg，以下媒体集成实验通过：
  - 5 分钟、9,000 帧的合成素材生成 10 个切片，每段 900 帧。
  - 901 帧的合成素材生成 900 帧与 1 帧的两段，保留真实尾帧。
  - 编码器缺失时正确返回失败，不遗留最终结果目录或临时目录。

以上切片实验验证本地媒体模块；真实 HTTP 视频任务及下载验证另见第 8 节。
#### 6. 后端接口骨架

- 新增 api.py，使用 FastAPI 定义服务，通过 Uvicorn 在本地运行。
- 实现 GET /health，浏览器访问后返回服务状态。
- 验证 /docs 自动接口文档能够正常打开。
- 生成 requirements.txt，记录当前环境的依赖版本，pip check 检查通过。
- 保留健康检查和加法演示接口；新增视频上传、任务查询、结果下载接口，进度目前为阶段估计值。

#### 7. Docker 后台队列与 HTTP 联调

- 使用 Docker Compose 部署 Redis、Celery worker 和 FastAPI；worker 在 Linux 容器中运行。
- 命令行队列实验通过：提交 2 + 3，后台执行后取得结果 5。
- 实现 `POST /demo/tasks`，返回 HTTP 202、任务编号与查询地址；请求不等待计算结束。
- 实现 `GET /demo/tasks/{task_id}`，读取任务状态快照与结果。
- 2026-09-21 在本机运行 `tests/integration_queue_api.py --base-url http://127.0.0.1:8200`，实际观察到 PENDING → STARTED → SUCCESS，结果为 5；非法参数、非法编号及未知编号语义检查通过，脚本输出 PASS。
- ClipForge 使用宿主机 8200 端口，映射容器内 8000；Redis 不开放宿主机端口。
- 记录 PENDING 可能表示未执行、未知编号或结果过期，不能代替任务数据库的存在性判断。

#### 8. 视频后台任务与结果下载

- 新增 `POST /tasks` 上传视频并返回任务编号；Celery 后台依次读取元数据、归一化、按 900 帧切片和打包。
- 新增 `GET /tasks/{task_id}` 查询状态，以及按任务结果清单下载 MP4、JSON 和 ZIP 的接口。未知视频任务返回 404。
- 引入 SQLite 保存视频任务状态，API 和 worker 共享 `media_data` 数据卷。实现阶段进度、失败记录、重复投递保护和未完成结果下载保护。
- worker 配置改用 Trixie 基础镜像安装 FFmpeg 7.1 系列；实际运行版本仍需将 `ffmpeg -version` 输出归档。
- 使用真实素材 `Test1.mp4`（345.5 秒）提交 Docker 后台任务，任务编号 `8e9ae99c-68f4-441a-b635-b9947dedf537`，处理状态为 SUCCEEDED，ZIP 可下载。
- 独立复核该已完成任务：共 12 段，前 11 段各 900 帧（30 秒），末段 465 帧（15.5 秒），总计 10,365 帧，清单帧区间连续。
- ZIP CRC 和文件清单检查通过；使用本机 FFprobe 解码计数，12 段均与计划一致，视频均为 H.264、平均帧率 30/1，音频均为 AAC。
- 自动验证记录保存于 `docs/samples/Test1_video_task_verification.json`，不将视频和 ZIP 成品提交到 Git。
- 原集成脚本因误用 `--expected-clips 2` 停止，不能记为该脚本完整 PASS；本次通过查询已有任务、重新下载 ZIP 与逐段核验补充验证，没有重复上传。
- 人工播放检查：待补充确认（用户已确认结果下载基本正常）。

#### 9. 浏览器操作页面与实际部署验收

- 新增 `index.html`，首页提供视频上传、历史任务、处理阶段和 ZIP / 单段下载入口，无需手工拼接下载链接。
- 新增 `GET /tasks` 分页查询历史任务；所选任务编号保存在地址栏，刷新后重新查询状态。
- 新增 4 项页面与历史接口测试，2026-09-22 在本地仓库运行全部 43 项测试通过。
- 在实际 Docker 服务 `http://127.0.0.1:8200/` 中，通过页面上传 `Sucai1.mp4`，任务编号 `1a0b0b8d-2d9a-45de-903a-e77e3dca1e04`。
- 页面观察到“生成切片 / 60%”，随后刷新，仍选中同一任务并显示完成，出现 30.00 秒与 11.27 秒的两段结果。
- 点击“下载全部切片 · ZIP”触发浏览器下载事件，浏览器控制台未捕获错误；原 Test1 历史任务仍显示在列表中。
- 独立下载新任务 ZIP 并复核：CRC、文件清单与连续帧区间通过；两段解码计数为 900 和 338，均为 H.264 / AAC、平均帧率 30/1。
- 验证记录：`docs/samples/web_ui_video_task_verification.json`。本次刷新验收不等同于服务重启或处理中任务中断恢复验收。

#### 10. 重启操作后的已完成任务与下载文件检查

- 2026-09-22，重启前确认没有未完成的视频任务，保存两个已完成任务的状态、历史编号和 ZIP SHA-256 校验值。
- 向用户提供 `docker compose restart redis worker api` 步骤后，用户反馈页面仍有记录并提供截图；本次未独立归档 Docker 重启命令输出。
- 随后重新请求接口：Test1.mp4 与 Sucai1.mp4 均保持 SUCCEEDED，分别为 12 段、2 段，任务记录与之前一致。
- 重新下载两个 ZIP，文件长度及 SHA-256 校验值均与之前一致，历史列表保留原任务。
- 验收记录保存于 `docs/samples/restart_persistence_verification.json`。
- 验证范围限于已完成任务。未验证处理中任务中断后的自动恢复、容器重建、电脑重启或数据卷删除场景。

#### 11. 长视频流程验收与交付文档整理

- 已生成并读取参数确认 60 分钟、1920×1080、30 fps 的合成测试素材，画面为蓝色背景并带测试音。
- Docker 任务 `782b8db7-3b4c-40db-b749-90416103eec0` 已 SUCCEEDED，原集成脚本完整 PASS；生成 120 段，独立逐段解码均为 900 帧，总计 108,000 帧，清单连续、ZIP CRC 与文件清单检查通过。
- 两次下载 ZIP 的 SHA-256 一致；任务记录创建至完成为 3017.892 秒（约 50 分 18 秒），对素材时长比值 0.8383，不含上传与下载。全程峰值内存及完整端到端耗时未归档，不宣称完整性能要求通过。
- 新增 `docs/LONG_VIDEO_TEST.md` 与 `docs/samples/long_video_verification.json`，保留逐段证据和低复杂度素材的验证限制。
- 更新 README，补充第二周功能、分支选择、启动与使用步骤、真实测试入口和已知限制；保留第一周调研文档入口。
- 新增 `docs/WEEK2_REVIEW.md`，整理老师查看的演示顺序、验收矩阵、Git 提交记录与未完成项。
- 文档整理时 CI 仍为占位任务；后续接入工作见第 12 节。本地通过不等于 GitHub 已运行通过。

#### 12. 真实持续集成配置

- 将原占位工作流替换为 `ClipForge checks`，推送和拉取请求时，在 Ubuntu / Windows 上分别使用 Python 3.11 检查依赖、运行 Ruff 与 43 项单元测试。
- 新增 `ruff.toml` 与固定版本开发依赖，先检查语法、部分控制流及未定义名称错误；不自动改写代码格式。
- 新增 `docs/CI.md`，说明本地复现命令、GitHub 查看方法、失败排查及验证范围。
- 本地预验收通过；提交 `d70eb43` 的[GitHub 运行](https://github.com/TiAmoLovee/video-editor-research/actions/runs/35679910293)已核验 Ubuntu / Windows 两个任务的依赖检查、Ruff 与单元测试步骤全部成功。不将本地长视频、Docker 或 Redis 放入单元测试工作流。
- 本次尚未采集覆盖率，不能声称达到需求中的覆盖率目标。

#### 13. 实际部署异常输入验收

- 通过实际页面上传刻意制作的无效 MP4，后台在 probing 阶段记为 FAILED；页面显示失败，刷新保留记录，没有下载链接。
- 直接请求失败任务的 ZIP 与 MP4 均返回 409；无效扩展名上传返回 415，空 MP4 返回 422。
- 不重启服务，随后提交 1 秒正常无音轨视频，任务 SUCCEEDED，生成 1 段，ZIP CRC 与文件清单通过；页面显示正常下载入口。
- 新增 `tests/integration_failure_api.py`、`docs/FAILURE_TESTS.md` 与 `docs/samples/failure_verification.json`，保留可复现脚本和实际证据。
- 脚本通过 Ruff；本实验不验证强制中断恢复、磁盘不足或大文件资源边界。
- 上一笔长视频记录提交 `a30d8ec` 的 [CI](https://github.com/TiAmoLovee/video-editor-research/actions/runs/35681686427)已确认两平台成功；本轮修改的 CI 需推送后单独确认。

### 未完成及原因

- **任务可靠性**：上传、共享存储、媒体处理和 SQLite 状态已实现；队列投递与数据库写入尚无事务补偿，处理中任务中断后的自动恢复、取消与重试尚未实现。
- **集成验证扩展**：真实视频上传、Docker 后台处理、查询和下载已验证 Test1 与 Sucai1 两例；60 分钟合成素材集成脚本已完整 PASS；无效扩展名、空文件、损坏输入与后续正常处理也已验证。资源故障和更多边界素材仍待扩大覆盖。
- **长视频与恢复能力**：页面刷新找回任务、重启操作后已完成任务与文件对比已检查；60 分钟低复杂度合成素材流程已通过，但全程峰值内存、完整端到端性能和处理中断恢复仍未验证。重启证据范围见第 10 节。
- **验证完善**：尚未接入自动 JSON Schema 校验；逐帧 CFR 检查尚未纳入常规自动化测试；切点处音频衔接与音画同步误差尚未进行自动测量。
- **持续集成**：已配置 Ubuntu / Windows 的依赖检查、Ruff 与单元测试，本地及首次 GitHub 运行已通过；覆盖率统计尚待完成。

### 技术障碍与当前假设

- Python、FFprobe 和 FFmpeg 已能在 Windows 本机运行；Redis、Celery worker 与 FastAPI 容器已完成加法任务及真实视频任务联调。
- worker 使用容器内 FFmpeg；API 与 worker 通过同一个数据卷访问上传文件、任务数据库和处理结果。Windows 的 `D:\Shotcut` 路径只用于本机媒体检查。
- Windows 本机使用 Shotcut FFmpeg 8.1 系列，worker 配置采用 Debian Trixie 的 FFmpeg 7.1 系列；不能将一次素材通过视为所有版本、编码和时长的兼容性验收。

### 失败的尝试

- **Git 直连推送失败**：出现连接重置或无法连接 GitHub 的错误。显式使用本地代理，并完成 Git 身份认证后，推送成功。
- **安装后终端找不到 Python**：`python` 命令仍指向 Windows 商店入口。确认实际安装成功后，改用 `py -3.11` 和项目虚拟环境中的解释器，运行成功。
- **工作记录未进入暂存区**：文件保存时间晚于此前的 `git add`，导致提交预览中缺少该文件。保存后重新暂存，修改正常进入待提交内容。

- **Docker 镜像拉取超时**：Docker Hub 认证和下载曾超时，明确配置 Docker Desktop 的本地 HTTP 代理后成功拉取基础镜像并构建服务。
- **API 端口冲突**：宿主机 8000 已被 Docker 服务占用；将 ClipForge 映射改为 `127.0.0.1:8200:8000`，启动及 HTTP 集成测试通过。
- **Compose 配置缺少服务**：一次检查发现配置仅保留 Redis，补齐 worker 与 api 后恢复启动。

- **Trixie 镜像构建认证超时**：构建过程获取 Docker Hub 令牌失败，在当前 PowerShell 设置 HTTP_PROXY、HTTPS_PROXY 和本地地址 NO_PROXY 后继续构建成功。
- **切片数量测试预期错误**：更换为 345.5 秒素材后仍指定 `--expected-clips 2`，实际正确生成 12 段，导致测试断言失败。更换素材时应按时长设置预期，或不指定固定段数。

### 下周计划

- 根据第二周闭环完成情况，安排第三周的镜头检测、语音活动检测和语音转写。
- 先完成第二周剩余验收：完整长视频性能记录和资源故障场景，并补齐运行环境版本记录，再推进结构分析功能。