# 60 分钟 1080p 长视频验收

日期：2026-09-22。结论：**本次合成素材的上传、后台处理、120 段切片与 ZIP 下载流程通过，全部切片的实际解码帧数核验通过。** 完整性能验收仍缺少资源数据，不将本次结果扩大为所有长视频的性能保证。

## 素材与执行方式

- 素材：`long_60min_1080p.mp4`，3600 秒，1920×1080，30 fps，H.264 / AAC，48 kHz 单声道。
- 内容：均匀蓝色画面和 440 Hz 测试音，视觉复杂度低；不代表复杂实拍内容。
- 使用既有 Docker API、Celery worker、Redis 和共享数据卷，测试命令如下：

```powershell
.\.venv\Scripts\python.exe backend\tests\integration_video_api.py "downloads\fixtures\long_60min_1080p.mp4" --expected-clips 120 --timeout 7200
```

- 任务编号：`782b8db7-3b4c-40db-b749-90416103eec0`，最终状态为 `SUCCEEDED`。
- 原集成日志实际输出 `PASS: upload -> worker -> 120 clips -> ZIP verified`；与之前 Test1 的错误预期测试不同，本次脚本完整通过。

## 输出核验

| 项目 | 结果 |
| --- | --- |
| 切片数量 | 120 |
| 每段实际解码视频帧数 | 全部 900 帧 |
| 总视频帧数 | 108,000 帧 |
| 清单帧边界 | 从 0 到 108,000，连续，无帧区间空隙或重叠 |
| 各段视频参数 | 1920×1080、H.264、平均帧率 30/1 |
| 各段音频参数 | AAC、48 kHz、单声道 |
| ZIP | CRC 通过；精确包含 120 个 MP4 和 3 个 JSON |
| 重复下载 | HTTP 重新下载的 ZIP 与集成脚本下载的 ZIP SHA-256 相同 |

助手通过本机 FFprobe 对 120 段逐一解码计数，不仅依赖清单中的帧数。此检查不会重新提交转码任务。详细工具版本、文件校验值和逐段结果见[验收 JSON](samples/long_video_verification.json)。

## 耗时口径

- 数据库任务创建：2026-09-22 **09:47:09**（UTC+8，上传文件保存之后）。
- 数据库记录完成：2026-09-22 **10:37:26**（UTC+8）。
- 两个完整时间戳之差：**3017.892 秒，约 50 分 18 秒**。
- 上述耗时 ÷ 3600 秒素材时长：**0.8383**。

这个比值采用“任务记录创建至完成”的口径，包含排队与后台处理，不含上传传输、ZIP 下载和本次独立复核。它不是完整端到端 RTF，不据此直接认定需求中的 CPU / GPU 性能目标通过。完整端到端耗时未归档。

## 尚未测量

- 全程峰值内存没有采集，记录为未测量，不能用结束后的瞬时内存代替。
- 没有独立归档容器 OOM / 重启计数或完整硬件与资源限制；本次只确认任务成功完成。
- 本次检查平均帧率及实际解码帧数，没有逐帧测量 CFR 时间戳、切点音频衔接或音画同步误差，也未记录人工播放验收。
- 复杂实拍视频、任务强制中断恢复和多任务压力需另外验证。

## 记录位置

`docs/samples/long_video_verification.json` 可提交 Git。原视频、下载 ZIP、集成日志与逐段复核临时文件不提交 Git。第二周整体状态见[成果与验收说明](WEEK2_REVIEW.md)及[周报](WEEKLY.md)。
