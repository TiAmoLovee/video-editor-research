[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "clipforge-backend"
version = "0.5.0"
description = "ClipForge local video processing backend"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["clipforge*"]
